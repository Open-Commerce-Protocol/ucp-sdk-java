/**
 * Optional, non-opinionated helper utilities for UCP.
 *
 * <p>This package only contains constants and string helpers (headers, routes, well-known capability
 * identifiers). It does not implement any transport (HTTP/MCP/Webhook) behavior and does not encode
 * any agent flow/state machine logic.
 */
package io.deeplumen.ucp.helpers;

